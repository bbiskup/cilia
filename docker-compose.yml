version: '2'
services:
    cilia-dev:
        build:
            context: .
            dockerfile: Dockerfile.cilia-dev
        network_mode: host
        volumes:
            # - ./data:/data
            - ./examples/cilia.yml:/home/cilia/cilia.yml